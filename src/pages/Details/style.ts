import styled from "styled-components";

export const Container = styled.div`
  .buttonBackPage {
    font-size: 1.6rem;
    font-weight: 500;
    position: absolute;
    top: 200px;
    left: 42px;

    button:hover {
      text-decoration: underline;
    }
  }
`;
